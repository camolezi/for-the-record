import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.fortherecord',
  appName: 'fortherecord',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
